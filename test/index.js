import mocha  from 'mocha'
import sinon  from 'sinon'
import assert from 'power-assert'
import mizukiri from '../index.js'

describe('index', () => {
  it('import', () => assert(mizukiri !== null))
})

